#psuedocode for the points system
import time
points = o
#countdown and answers need to be integrated seperately from each other? because your setting seconds to score points
if answer in answers:
    print("correct")
    def countdown_timer(seconds):
        seconds = 20
        #needs to a threads
        while seconds > 0:
            print(f"Time remaining: {seconds} seconds")
            time.sleep(1)
            seconds -= 1
   
    if countdown > 18:
        points = max
    if countdown > 15 and countdown < 18:
        points = second_highest
    10 - 15
    10 -5 
    5-1
    if countdown = 0:
        points = points + 0
        next question

   